<Window x:Class="Tippy.App.AdvancedFeaturesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tippy Advanced Features" Width="900" Height="720" MinWidth="720" MinHeight="560"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource TextBrush}" Icon="/Tippy;component/Assets/Icons/tippy.ico">
    <Grid Margin="22">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="Advanced Features" FontSize="25" FontWeight="SemiBold"/>
            <TextBlock Text="Everything Tippy can do beyond a basic one-key pedal—organized in one searchable place."
                       Style="{StaticResource SmallMutedText}" Margin="0,4,0,0"/>
        </StackPanel>
        <TextBox Grid.Row="1" x:Name="SearchBox" Margin="0,18,0,12" TextChanged="SearchBox_TextChanged"
                 ToolTip="Search features"/>
        <ListBox Grid.Row="2" x:Name="FeaturesList" BorderThickness="0" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemsPanel><ItemsPanelTemplate><UniformGrid Columns="2"/></ItemsPanelTemplate></ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/><Setter Property="Margin" Value="0,0,10,10"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate><DataTemplate>
                <Button Click="Feature_Click" Tag="{Binding}" AutomationProperties.Name="{Binding Title}"
                        HorizontalContentAlignment="Stretch" Padding="15" MinHeight="118">
                    <Grid>
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                        <TextBlock Text="{Binding Group}" Style="{StaticResource SmallMutedText}" FontWeight="Bold"/>
                        <TextBlock Grid.Row="1" Text="{Binding Title}" FontSize="17" FontWeight="SemiBold" Margin="0,5,0,0"/>
                        <TextBlock Grid.Row="2" Text="{Binding Description}" Style="{StaticResource SmallMutedText}" TextWrapping="Wrap" Margin="0,6,0,0"/>
                    </Grid>
                </Button>
            </DataTemplate></ListBox.ItemTemplate>
        </ListBox>
        <Grid Grid.Row="3" Margin="0,12,0,0">
            <TextBlock x:Name="CountText" Style="{StaticResource SmallMutedText}" VerticalAlignment="Center"/>
            <Button Content="Close" IsCancel="True" HorizontalAlignment="Right"/>
        </Grid>
    </Grid>
</Window>
